import React from "react"
import './Header.css'

export const Header = () => {
    return(
        <div className={'header'}>
            <img className={'header__img'} src={'images/instagram.png'} alt={''} />
        </div>
    )
}